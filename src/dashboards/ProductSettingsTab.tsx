import { BaseDashboard } from "@microsoft/teamsfx-react";

export default class ProductSettingsTab extends BaseDashboard<any, any> {
  override layout(): JSX.Element | undefined {
    return (
      <> 
      </>
    );
  }
}
